    3 fundamental files to make json Bible
        csv or json verses
        headings
        paragraph markers
------------------------------------------------

Base Bible
    json to formatted text

Commentary
    json to formatted text

Dictionary
    words
        word entry
        meaning
        people references
        location

Functions:
    In a textbook (object with keys/values):
        convert references to osis                  fomratTextBookOsisRefs.js
        tag osis adds tags to osis references       tagTextBook.js filename osis
        tag words adds tags to dictionary words     tagTextBook.js filename eastons

    In a file:
        convert references to osis                  formatTextOsisRefs.js
        tag osis adds tags to osis references       tagText.js filename osis
        tag words adds tags to dictionary words     tagText.js filename eastons



To identify right dictionary word
    If coming from Bible chapter, see if Bible chapter is in text
    If coming from Dictionary, see if there is a reference match in both word definitions
    If coming from person, use the person id



functions
    compileJSONBible(csv file);//paragraph markers and headings included in function
    convertReferences(text);
    addReferenceTags(text);
    addWordTags(text);




