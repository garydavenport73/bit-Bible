fundamental inputs to the program are

    CONSTANTS
    redLetterReferences.json
    readingPlans.js
    jfb.json
    eastons.json
    baseMap.js    
    bsb.json

    need function that takes csv and converts to
        nestedBible
        regularBible

    need to make an osis pulpit commentary

    *** Need Master CSV File
    *** Need a script that uses Bible abbreviation as input and uses Master CSV to make a nestedBible

    *** Extras:
        Need a script that uses Bible abbreviation as input and uses Master CSV to make json verses by osis key
        Need a script that uses Bible abbreviation as input and uses Master CSV to make a text bible by book and chapter